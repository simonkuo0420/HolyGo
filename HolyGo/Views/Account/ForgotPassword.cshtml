@model HolyGo.Models.ForgotPasswordViewModel
@{
    ViewBag.Title = "忘記密碼?";
}
<link rel="stylesheet" href="~/Assets/Css/Account/Login/Login.css" />
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/material-design-iconic-font/2.2.0/css/material-design-iconic-font.min.css" />

<div class="limiter">
    <div class="container-login100 pt-5">
        <div class="wrap-login100 p-5 pt-5 mt-5">
            @using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
                @Html.AntiForgeryToken()
            <span class="login100-form-title pb-5 h4">
                忘記密碼?
            </span>

                <div class="wrap-input100 validate-input mb-3" data-validate="Username is reauired">
                    <span class="label-input100">Username</span>
                    @Html.TextBoxFor(model => model.Email, new { @placeholder = "Type your username", @class = "input100" })
                    <span class="focus-input100" data-symbol="&#xf15a;"></span>
                </div>
                @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })

                <div class="container-login100-form-btn btn mt-2">
                    <div class="wrap-login100-form-btn">
                        <div class="login100-form-bgbtn"></div>
                        <button class="login100-form-btn" type="submit">
                            以電子郵件傳送連結
                        </button>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

@*@using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
    {
        @Html.AntiForgeryToken()
        <h4>輸入您的電子郵件。</h4>
        <hr />
        @Html.ValidationSummary("", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(m => m.Email, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" class="btn btn-default" value="以電子郵件傳送連結" />
            </div>
        </div>
    }*@

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
